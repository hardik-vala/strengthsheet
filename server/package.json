{
  "name": "strengthsheet-server",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node -r tsconfig-paths/register' src/server.ts",
    "start": "ts-node -r tsconfig-paths/register src/server.ts"
  },
  "dependencies": {
    "@types/express": "^4.17.18",
    "@types/node": "^20.7.0",
    "cors": "^2.8.5",
    "date-fns": "^2.30.0",
    "express": "^4.18.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.14",
    "nodemon": "^3.0.1"
  }
}
